name: 'H37kouya-label-demo'
description: 'Wait a designated number of milliseconds'
inputs:
  repo-token:
    description: 'GitHub Token'
    required: true
  asana-token:
    description: 'GitHub Token'
    required: true
outputs:
  time: # output will be available to future steps
    description: 'The message to output'
runs:
  using: 'node12'
  main: 'dist/index.js'
